name: GitHub - Activity - Readme

on:
  schedule:
    - cron: "*/1 * * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: suraj-davariya/github-activity-readme@master
        env:
          github_token_1: ${{ secrets.GITHUB_TOKEN }}
